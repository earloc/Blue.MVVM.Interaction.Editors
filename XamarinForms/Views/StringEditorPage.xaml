<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Blue.MVVM.Interaction.Editors.Views.StringEditorPage">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
    </Grid.RowDefinitions>

    <TableView>
      <TableRoot>
        <TableSection>
          <EntryCell Label="{Binding Prompt}" Placeholder="{Binding Placeholder}" Text="{Binding Value}" />
        </TableSection>
      </TableRoot>
    </TableView>
    <StackLayout Grid.Row="1" Orientation="Horizontal">
      <Button Text="Ok" Command="{Binding PrimaryCommand}"/>
      <Button Text="Cancel" Command="{Binding SecondaryCommand}"/>
    </StackLayout>
  </Grid>
</ContentPage>